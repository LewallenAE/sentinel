# Sentinel System Sketch

Sentinel is an AI infrastructure system for eval harnesses, RL environments, tool-use trajectories, rewards, and post-training artifacts.

Core loop:

Task → Agent Attempt → Tool Call → Observation → Oracle / Reward → Trajectory → Eval Result → Training Artifact → API / Dashboard